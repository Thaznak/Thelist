Place your real photo dataset in this folder.

Required:
- Keep `manifest.js` updated with 24 entries.
- For each entry, set `file` to a real image file name in this folder (`.jpg`, `.jpeg`, or `.png`).
- Keep stable `id` values and complete `attrs` so questions can work.

If an image file is missing, the game will show a generated placeholder for that card.
